language: python
python:
   - "3.6"

services:
  - docker

install:
  - touch varsettings.env
  - echo "SECRET_KEY=Thisisatestkey" >> varsettings.env
  - echo "DEBUG=True" >> varsettings.env
script:
  - docker-compose up  && docker exec -ti thewallproject_web_1 bash -c "cd thewall;python3 manage.py test"
